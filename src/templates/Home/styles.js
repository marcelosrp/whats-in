import styled from 'styled-components'

export const Search = styled.section`
  position: relative;
  width: 100%;

  svg {
    color: #a1a1a1;
    font-size: 1.6rem;
    left: 1rem;
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
  }
`
